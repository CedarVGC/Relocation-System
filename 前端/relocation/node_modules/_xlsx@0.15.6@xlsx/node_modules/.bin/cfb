#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_cfb@1.1.4@cfb/bin/cfb.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../_cfb@1.1.4@cfb/bin/cfb.njs" "$@"
  ret=$?
fi
exit $ret
